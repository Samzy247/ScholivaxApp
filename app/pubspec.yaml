name: scholivax_app
description: Scholivax School Management — mobile app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  connectivity_plus: ^6.0.3
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  flutter_inappwebview: ^6.1.5
  fl_chart: ^0.68.0
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
