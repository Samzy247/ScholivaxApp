<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Scholivax"
        android:theme="@style/Theme.Scholivax"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.teacher.TeacherHomeActivity" android:exported="false" />
        <activity android:name=".ui.teacher.AttendanceScanActivity" android:exported="false" />
        <activity android:name=".ui.teacher.MarksEntryActivity" android:exported="false" />
        <activity android:name=".ui.parent.ParentHomeActivity" android:exported="false" />
        <activity android:name=".ui.student.StudentHomeActivity" android:exported="false" />
        <activity android:name=".ui.admin.AdminHomeActivity" android:exported="false" />
        <activity android:name=".ui.common.CircularsActivity" android:exported="false" />

        <service
            android:name=".fcm.ScholivaxMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="scholivax_default" />

    </application>
</manifest>
